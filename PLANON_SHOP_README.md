# Planon Shop / Market beta

## 이번 버전에서 실제로 동작
- 설정 화면의 Planon Shop 진입
- 공식 상품 카탈로그 (가격은 `market/catalog.js`의 `P` 객체에서 수정)
- 무료 테마 5종
- 유료 테스트 상품/번들
- 10초 미리 적용
- 찜 / 보유함 / 테스트 구매 기록
- 관리자 `clover_cys@naver.com`은 현재 및 앞으로 카탈로그에 추가되는 모든 상품 자동 보유
- 테마/시간표/배경/체크/캐릭터/위젯/회고 프레임/주간 이미지 스타일 실제 적용
- 세팅팩 적용 + 적용 전 되돌리기
- Creator 신청 초안 UI

## 결제
현재 `market/payment-adapter.js`는 테스트 어댑터입니다. 실제 돈은 결제하지 않습니다. 추후 이 파일만 실제 결제 API 어댑터로 교체하고 서버 entitlement를 연결할 수 있게 분리했습니다.

## Market 확장
모든 상품에 `creatorId`, `creatorName`, `category`, `productType`, `price`, `forSale`, `thumbnail`, `description`, `payload` 구조가 있습니다.

## 무료 원칙
시간표, 할 일, 시험, 일정, 친구, 백업/복원, 자동 계획 등 기존 핵심 기능은 Shop 구매 여부로 제한하지 않습니다.


## 2026-09-25 통합 반영
- 친구 화면의 약속 잡기를 핵심 기능으로 다시 고정: 홈 빠른 버튼 설정과 무관하게 친구 상세/링크 약속에서 사용 가능
- 약속 종료 후 `언제·어디서·누구와` 만났는지 확인 → 한 명이 `응, 맞아`만 눌러도 해당 약속이 추억으로 즉시 기록
- 네모 바쁨 문구를 짧고 모바일 줄바꿈 안전하게 수정
- 할 일의 과목명은 제목 옆 배지 대신 아래 보조정보 줄로 정리
- 과목별 `이번 주 공부 시간`을 작업 시작/끝 기록에서 자동 합산해 시간표 과목 목록과 할 일 보조정보에 표시
- 오늘의 `어제의 내가 남긴 말`은 상단에 한 번만 표시하는 중복 방지 유지
- Planon Shop 구매내역 탭 추가
- 기존 안정성 수정(save 용량 복구, 통합 Supabase schema/migration, 계정삭제 RPC, privacy.html, PWA shell cache) 유지

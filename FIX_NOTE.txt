빈 화면 원인

GitHub 파일 목록의 순서는 문제가 아닙니다.
브라우저는 index.html 안의 script 순서대로 읽습니다.

실제 원인은 파일 분리 후 JavaScript 스코프가 갈라진 것이었습니다.
nemo.js / recipes.js / selfchat.js가 원래 보던 S/U/M/esc 등의 app-core 상태를
더 이상 못 보면서 일간 본문 렌더가 중간에 멈췄습니다.

수정:
app-core.js에서 첫 render 전에 필요한 상태와 helper를 외부 모듈에 연결했습니다.
